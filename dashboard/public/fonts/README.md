IBM Plex Sans Variable, Latin weight axis (100–700).

Source: `@fontsource-variable/ibm-plex-sans` version 5.3.0, file
`files/ibm-plex-sans-latin-wght-normal.woff2`. The UI uses weights 400–700.
The included OFL.txt is the package's original SIL Open Font License.
Chinese glyphs use the system font fallback; font loading never calls an external host.
