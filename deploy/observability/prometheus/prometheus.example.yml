global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/routepilot-alerts.yml

scrape_configs:
  - job_name: routepilot
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials_file: /run/secrets/routepilot_metrics_key
    static_configs:
      - targets:
          - routepilot:38082

  # Optional readiness probe. Run Blackbox Exporter with
  # blackbox.example.yml and mount the same dedicated, scoped probe key.
  - job_name: routepilot-readiness
    metrics_path: /probe
    params:
      module:
        - routepilot_ready
    static_configs:
      - targets:
          - http://routepilot:38082/readyz
    relabel_configs:
      - source_labels:
          - __address__
        target_label: __param_target
      - source_labels:
          - __param_target
        target_label: instance
      - target_label: __address__
        replacement: blackbox-exporter:9115
