# RoutePilot 全面改名实施记录

日期：2026-09-08。本文由用户确认的实施计划转为执行记录，保留原文件位置作为权威入口。

## 已确认范围

展示名为 `RoutePilot`，基础小写标识为 `routepilot`，环境变量前缀为 `ROUTEPILOT`。代码、数据库、HTTP 头、浏览器状态、文件路径、部署配置、监控、文档与图片统一命名。复合名称遵守所在语言的分隔约定。

开发数据可重新初始化，不提供旧品牌配置、数据库、Cookie 或备份兼容。保留 Git 历史与既有工作区成果。不提交、不推送、不发布镜像或版本、不部署生产环境。

仓库为 `lizzjin/RoutePilot`，发布镜像命名为 `ghcr.io/lizzjin/routepilot`，前端与运维代理使用对应后缀。本地验证镜像使用 `routepilot:local`、`routepilot-dashboard:local`、`routepilot-ops-agent:local`。

## 基线与成果保护

- 分支：`codex/routepilot-ui-redesign`；HEAD：`b847b0b948968b6ddde95611809fc2e9a6099159`。
- 修改前完整备份 612 个项目文件，覆盖未跟踪的工作台组件、测试、文档和图片；同时保存未暂存差异、暂存差异及原始 Git 索引。
- 私有基线目录：`/var/folders/sf/1077dvrx5mz86gs5kggbx9v00000gn/T/routepilot-rename-baseline-tdgqfxxo`。该目录含原始名称及原演示配置，仅用于恢复，不属于当前维护项目的名称扫描范围。临时目录可能被系统清理。
- 按文件重命名映射核对，基线文件无缺失；暂存区原始索引未改动。没有重置、切换工作分支或重写历史。
- 扫描包含隐藏文件和本地项目配置；排除 Git 内部、第三方依赖、构建缓存及验证临时输出。项目根目录原本没有本地 `.env`。

## 实施结果

| 阶段 | 结果 |
| --- | --- |
| 基线与盘点 | 完成。保存工作区与准确资源清单，确认 GitHub 管理权限及目标名称无冲突 |
| 代码与配置 | 完成。首轮更新 183 个文本文件，同步 Rust 包和导入、锁文件、环境变量、API 头、Cookie、浏览器状态、业务标识、13 个初始化迁移及备份格式 |
| 文档与资源 | 完成。重命名 8 个路径，统一仓库与镜像引用，更新品牌约束，复拍受影响的历史对照图及当前页面 |
| 隔离验证 | 完成。最终三种镜像构建及组合启动检查通过 |
| 外部仓库 | 完成。GitHub 实际改名、本地 origin 更新，远程 HEAD 可读取；未推送工作区 |
| 本机收尾 | 完成。工作区已迁移，本机项目路径配置已同步，测试资源已清理，演示预览已恢复 |

数据库表、索引、约束及相关查询一并更新，只在全新隔离 PostgreSQL 中验证。没有向原演示数据库运行修改后的迁移。

额外构建修正：主 Dockerfile 补入编译所需的 `catalog/`。Rust 1.96 的 Clippy 检出两处原有八参数函数；在函数局部添加带原因的 `expect(clippy::too_many_arguments)`，保留其接口和运行逻辑，没有全局降低 Lint 要求。

## 验证结果

| 检查 | 实际结果 |
| --- | --- |
| 前端 `npm --prefix dashboard run check` | Node 24.19.0；TypeScript、零警告 ESLint、29 个文件 / 109 项单测、生产构建通过 |
| Rust 格式 | Rust 1.96.0，`cargo fmt --all -- --check` 通过 |
| Rust 测试 | 全新隔离数据库，`cargo test --locked --all-targets -- --test-threads=1`，451 项通过，包含 PostgreSQL 测试及迁移集成测试 |
| Rust Clippy | `cargo clippy --locked --all-targets --all-features -- -D warnings` 通过 |
| 浏览器回归 | 31 项一次全量通过，包含权限、窄屏、编辑保护、一次性密钥、浏览器历史及用户关联密钥交接 |
| 全新初始化 | PostgreSQL 18.4，13 个迁移成功、18 张新命名业务表，后端可启动和登录 |
| 备份 | 新格式导出、校验、恢复到另一全新数据库再导出；认证与控制文档逐项相等；旧品牌备份被拒绝 |
| HTTP 与会话 | 新 Cookie 名生效；旧 CSRF 头返回 403，新 CSRF 头返回 200；存活、认证就绪及模型目录检查通过 |
| 小团队验收脚本 | Linux 容器内通过用户/团队/密钥、IP 限制、消费限额、审计、备份及清理流程；使用明确标注的临时内部测试价格卡，不调用真实上游 |
| 配置示例 | 3 个环境示例和 2 个 TOML 示例通过实际 CLI 校验 |
| 部署 | 开发、发布及生产 Compose 模板渲染通过；Linux 生产配置预检、Nginx 配置检查通过；三种最终镜像构建及前后端组合登录检查通过 |
| 监控 | Prometheus 配置及全部 20 条告警规则通过 promtool 校验 |
| 文档与脚本 | Markdown 链接检查、27 个 Bash 脚本语法检查、`git diff --check` 通过 |
| 图片 | 219 张位图识别复核未命中旧品牌；20 张受影响历史图按对应版本复拍，39 张当前页面复拍，无页面溢出或运行时异常 |

测试环境问题已处理：全局 TLS 禁用与默认值单测冲突，改为只向 Rust 测试传数据库地址；共享数据库测试并行清表出现死锁，换用全新数据库串行复验。验收脚本需要可用模型和明确价格卡；初次临时配置缺少这两项，补齐隔离测试配置后通过。macOS 无法直接执行 Linux 二进制，GNU 工具检查及 CLI 验收在 Linux 容器内完成。

最终局部 Clippy 注解不改变运行逻辑；完整功能测试在注解前通过，注解后复验格式与 Clippy，重建最终镜像。

## 视觉证据

- [历史截图复拍清单](assets/routepilot-rename/history-captures.json)：对应原版本的独立源码副本，仅更新命名后重新渲染，保留原布局与视口。
- [当前页面复拍清单](assets/routepilot-rename/current-captures.json)：当前工作区配合新命名隔离后端，13 个路由、3 种视口，共 39 张。
- [当前截图检查](assets/workbench-redesign/implementation/capture-results.json)：无溢出和运行时异常。

复拍样本和时间会变化，不能把新图片冒充过去日期的验收证据。原始图片保留在 Git 历史或改名前私有基线备份中；此前工作台设计、权限、字段与操作保护约束继续有效。

## 外部与本机状态

GitHub 仓库为 [lizzjin/RoutePilot](https://github.com/lizzjin/RoutePilot)，origin 使用相应 HTTPS Git 地址。仓库没有配置 Actions 变量或密钥，没有 GitHub Pages；Actions 保持原有启用状态。配置中的新 GHCR 地址不代表镜像已经发布。改名后的项目文件尚未推送，远程分支仍是原有内容；新增文件路径在后续提交推送后才会在线生效。

最终工作区为 `/Users/zijinli/RoutePilot`。已同步本机 Codex 项目信任路径和 Claude 项目/仓库路径映射，保留原设置值与其他项目配置；修改前的配置副本位于私有基线目录。Codex 信任配置按项目路径索引，参见[官方配置参考](https://learn.chatgpt.com/docs/config-file/config-reference)。当前任务和应用中已保存的项目仍绑定迁移前目录，需要在应用中重新打开 `/Users/zijinli/RoutePilot` 继续工作；这是当前任务环境的路径绑定限制，不改写任务历史或应用内部数据库。

新目录下前端生产构建与 Docker 后端 builder 构建（缓存复用）均通过，Git HEAD 和 origin 检查通过。演示预览为 `http://127.0.0.1:33012/dashboard`，已复验登录、演示标记和新命名设置。使用仓库既有 mock 登录方式；这是演示数据，不连接正式服务。测试专用容器、网络、数据库和临时凭据均已清理。

原演示资源清理前复核容器挂载、Compose 标签、网络成员和卷引用，确认没有其他容器共用。原配置已另存到私有基线目录。清理仅涉及确认属于本项目的 4 个原容器、3 个原卷、1 个原网络、3 个原镜像标签及其配置临时目录，没有执行全局 prune。

## 验证边界

所有后端和浏览器验证使用隔离的本地环境；没有调用付费上游，不代表真实模型成功生成或生产部署认证。原演示数据库不保留在线服务或升级兼容。Git 历史、第三方依赖、构建缓存和恢复备份不纳入当前维护项目的旧品牌零残留要求。

本轮没有提交、推送、发布或生产部署。
